const startAfter = null; // eslint-disable-next-line
const serverTimestamp = null; // eslint-disable-next-line
const COGNITIVE_DOMAINS = []; // eslint-disable-next-line
const getGameMetadata = () => {}; // eslint-disable-next-line
let domainMetadata = {}; // eslint-disable-next-line

// Mock implementation for Firestore functions can be added here if needed.